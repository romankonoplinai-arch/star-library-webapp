import { useState, useEffect } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import { GlassCard, LoadingSpinner } from '@/components/ui'
import { api } from '@/lib/api'
import { useUserStore } from '@/stores'

interface FullAnalysisModalProps {
  isOpen: boolean
  onClose: () => void
  sunSign: string
  moonSign: string
  ascSign: string
}

const PAGES = [
  {
    title: 'Ваша Большая Тройка',
    content: (sun: string, moon: string, asc: string) => `
Знаете, когда Лунара впервые показала мне эти древние свитки, я подумал: "Опять эти мудрёные символы!" Но потом до меня дошло - это же ключ к вашей личности!

🌟 Солнце в ${sun} - это ваш внутренний стержень, ваша суть. Представьте, что это главный герой вашей истории. Тот, кто принимает важные решения и определяет вашу миссию в этом мире. Когда вы искренни с собой и действуете от сердца - это проявляется ваше Солнце!

🌙 Луна в ${moon} - ваш эмоциональный мир, те самые чувства, которые бурлят внутри. Это ваша чувствительность, интуиция и то, что помогает вам понять себя в тишине. Луна знает все ваши секреты и мечты, она шепчет вам, когда никто не видит.

✨ Асцендент в ${asc} - ваша "социальная маска", первое впечатление. Это как обложка книги - не вся история, но именно она привлекает внимание. То, как вы входите в новую комнату, знакомитесь с людьми, реагируете на неожиданности.

Вместе эти три силы создают уникальную симфонию именно ВАС. Понимание их взаимодействия - первый шаг к самопознанию!
    `
  },
  {
    title: 'Солнце: Ваша Суть',
    content: (sun: string, _moon?: string, _asc?: string) => `
Ах, Солнце в ${sun}! Лунара всегда говорит: "Меркурий, посмотри, как ярко оно сияет!" И правда, это не просто астрологический символ - это ваша жизненная энергия!

⭐ Ваше Солнце определяет, что даёт вам силы вставать каждое утро. Это ваш внутренний огонь, который горит независимо от обстоятельств. В ${sun} эта энергия проявляется особым образом - она окрашивает все ваши поступки и стремления.

Знаете, что самое интересное? Многие люди живут, не понимая своего солнечного знака. Они пытаются быть кем-то другим, носят чужие маски, забывая о своей истинной природе. А потом удивляются, почему жизнь не приносит радости!

Ваше Солнце в ${sun} - это ваша суперсила. Когда вы действуете в соответствии с ней, всё складывается легко и естественно. Сопротивляетесь - чувствуете внутренний конфликт.

💫 Представьте, что ваше Солнце - это режиссёр вашего фильма. Оно определяет жанр, основной сюжет, главные цели. Остальные планеты - актёры и декорации. Но именно Солнце задаёт тон всей истории!

Так что не бойтесь сиять так, как вам свойственно от природы. Мир нуждается именно в ВАШЕМ свете, а не в копии чужого.
    `
  },
  {
    title: 'Луна: Ваши Эмоции',
    content: (_sun: string, moon: string, _asc?: string) => `
Селена, наша загадочная хранительница снов, всегда говорит: "Луна видит то, что скрыто от дневного света." И она абсолютно права!

🌙 Ваша Луна в ${moon} - это ваш внутренний ребёнок, ваша эмоциональная правда. Это та часть вас, которая чувствует, мечтает, нуждается в заботе и понимании. Многие прячут свою Луну от мира, считая эмоции слабостью. Но знаете что? Это огромная ошибка!

Луна управляет вашими инстинктами, интуицией, тем, как вы реагируете на стресс и что вам нужно для эмоционального комфорта. В ${moon} эта энергия проявляется через особую призму восприятия мира.

Например, когда вам плохо, что вы делите? Кому-то нужно побыть одному, кому-то наоборот - окружить себя людьми. Кто-то заедает стресс вкусняшками (привет от Марсика!), а кто-то уходит в творчество. Всё это - ваша Луна в действии!

🌊 Эмоции - это не враг, а компас. Они показывают, что для вас важно, где вы чувствуете себя в безопасности, а где теряете энергию. Луна в ${moon} подсказывает, как найти свой внутренний покой.

Слушайте свою Луну. Она не обманывает. Когда что-то "не лежит душа" - это Луна предупреждает. Когда сердце поёт - это она радуется. Доверяйте своим чувствам!
    `
  },
  {
    title: 'Асцендент: Ваша Маска',
    content: (_sun: string, _moon: string, asc: string) => `
Аристарх, наш мудрый Хранитель Знаний, обожает объяснять Асцендент через метафору двери: "Это не просто вход в дом - это то, как дом представляет себя миру!"

✨ Ваш Асцендент в ${asc} - это ваша "социальная упаковка", стиль поведения, который вы используете при встрече с новыми людьми и ситуациями. Важно понять: это НЕ фальшь и НЕ обман!

Асцендент - это естественный защитный механизм. Представьте: вы заходите в незнакомую комнату. Вы же не начинаете сразу рассказывать свои самые сокровенные тайны? Вот тут и включается Асцендент - ваша адаптивная маска.

В ${asc} эта маска имеет особый характер. Она определяет, какое первое впечатление вы производите, какой энергией окружающие вас воспринимают. Кто-то видит вас серьёзным, кто-то весёлым, кто-то загадочным - всё зависит от вашего Асцендента!

🎭 Интересный факт: часто люди узнают в описании своего Асцендента не себя, а то, как их видят другие. "Я так себя не чувствую!" - говорят они. Именно! Потому что это внешний слой, не внутренний стержень (это Солнце) и не эмоции (это Луна).

Но не спешите отвергать свой Асцендент! Он - ваш помощник в социальном мире. Он помогает вам адаптироваться, находить контакт, защищать свою уязвимость. Уважайте свою маску - она служит вам верой и правдой!
    `
  },
  {
    title: 'Рекомендации',
    content: (_sun?: string, _moon?: string, _asc?: string) => `
Ну что, друзья, добрались до финальной страницы! Лунара попросила меня передать вам несколько важных мыслей:

🌟 1. УГЛУБЛЯЙТЕСЬ В КАРТУ
Большая Тройка - это только начало! В вашей натальной карте есть ещё Меркурий (общение), Венера (любовь), Марс (действия), Юпитер (удача), Сатурн (уроки) и многое другое. Каждая планета - это отдельная грань вашей личности. Не останавливайтесь на достигнутом!

🌙 2. НАБЛЮДАЙТЕ ЗА ТРАНЗИТАМИ
Планеты постоянно движутся, создавая новые энергии в вашей жизни. Иногда вы чувствуете прилив сил - это гармоничный транзит. Иногда всё валится из рук - возможно, Меркурий в ретроградности решил пошутить! Следите за небесными событиями.

✨ 3. РАСТИТЕ, А НЕ ОПРАВДЫВАЙТЕСЬ
Астрология - это инструмент самопознания и роста, а не отмазка! "Я такой потому что Скорпион" - это путь в никуда. Правильный подход: "Я Скорпион, значит у меня есть глубина и страсть, как мне использовать это во благо?"

📚 4. ПОМНИТЕ О СВОБОДЕ ВОЛИ
Карта показывает потенциал, склонности, уроки. Но выбор всегда за ВАМИ! Вы не марионетка планет, вы - сознательное существо с правом выбора. Звёзды подсказывают путь, но идёте вы сами.

🔥 5. ДЕЛИТЕСЬ ЗНАНИЯМИ
Чем больше людей понимают себя и других через астрологию, тем добрее становится мир. Не держите знания в себе - помогайте другим открывать их уникальность!

Удачи вам на пути самопознания! Двери Звёздной Библиотеки всегда открыты для вас. 🌌
    `
  }
]

export function FullAnalysisModal({ isOpen, onClose, sunSign, moonSign, ascSign }: FullAnalysisModalProps) {
  const [currentPage, setCurrentPage] = useState(0)
  const [direction, setDirection] = useState(0)
  const [aiInterpretation, setAiInterpretation] = useState<string | null>(null)
  const [isLoading, setIsLoading] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [parsedPages, setParsedPages] = useState<{ title: string; content: string }[]>([])
  const defaultCharacter = useUserStore((s) => s.defaultCharacter)

  // Reset and load AI interpretation when modal opens
  useEffect(() => {
    if (!isOpen) {
      // Reset state when modal closes
      setCurrentPage(0)
      setAiInterpretation(null)
      setParsedPages([])
      setError(null)
      return
    }

    // If we already have interpretation and parsed pages, don't reload
    if (aiInterpretation && parsedPages.length > 0) return

    const loadInterpretation = async () => {
      setIsLoading(true)
      setError(null)

      try {
        const response = await api.interpretNatalChart(
          sunSign,
          moonSign,
          ascSign,
          defaultCharacter
        )

        console.log('API Response:', response)

        // Check if interpretation exists
        if (!response || !response.interpretation) {
          throw new Error('AI не вернул толкование')
        }

        setAiInterpretation(response.interpretation)

        // Parse AI response into 5 pages
        const pages = parseAiInterpretation(response.interpretation)
        console.log('Parsed pages:', pages)
        setParsedPages(pages)
      } catch (err: any) {
        console.error('Failed to load AI interpretation:', err)
        setError(err.message || 'Не удалось загрузить толкование')

        // Fallback to static content
        setParsedPages(PAGES.map(p => ({
          title: p.title,
          content: p.content(sunSign, moonSign, ascSign)
        })))
      } finally {
        setIsLoading(false)
      }
    }

    loadInterpretation()
  }, [isOpen, sunSign, moonSign, ascSign, defaultCharacter])

  const parseAiInterpretation = (text: string): { title: string; content: string }[] => {
    // Safety check: if text is null/undefined/empty, return fallback
    if (!text || typeof text !== 'string' || text.trim().length === 0) {
      return [{
        title: 'Ошибка',
        content: 'Не удалось получить толкование от AI'
      }]
    }

    // Try multiple parsing strategies

    // Strategy 1: "Страница N - "Title""
    const pattern1 = /Страница \d+ - "([^"]+)"[:\s]*([\s\S]*?)(?=Страница \d+|$)/g
    let matches = [...text.matchAll(pattern1)]

    if (matches.length >= 3) {
      return matches.slice(0, 5).map(match => ({
        title: match[1],
        content: match[2].trim()
      }))
    }

    // Strategy 2: "**Title**" or "## Title"
    const pattern2 = /(?:\*\*|##)\s*(.+?)\s*(?:\*\*|##)?\s*\n([\s\S]*?)(?=(?:\*\*|##)|$)/g
    matches = [...text.matchAll(pattern2)]

    if (matches.length >= 3) {
      return matches.slice(0, 5).map(match => ({
        title: match[1].replace(/\*\*/g, '').trim(),
        content: match[2].trim()
      }))
    }

    // Strategy 3: Split by double newlines and take chunks
    const chunks = text.split(/\n\n+/).filter(chunk => chunk.trim().length > 50)
    if (chunks.length >= 3) {
      return chunks.slice(0, 5).map((chunk, i) => {
        const lines = chunk.split('\n')
        const title = lines[0].replace(/\*\*/g, '').replace(/##/g, '').trim()
        const content = lines.slice(1).join('\n').trim()
        return {
          title: title || `Часть ${i + 1}`,
          content: content || chunk
        }
      })
    }

    // Fallback: return whole text as one page
    return [{
      title: 'Ваша Натальная Карта',
      content: text.trim()
    }]
  }

  const nextPage = () => {
    if (currentPage < parsedPages.length - 1) {
      setDirection(1)
      setCurrentPage(currentPage + 1)
    }
  }

  const prevPage = () => {
    if (currentPage > 0) {
      setDirection(-1)
      setCurrentPage(currentPage - 1)
    }
  }

  if (!isOpen) return null

  const currentPageData = parsedPages[currentPage] || { title: '', content: '' }

  const pageVariants = {
    enter: (direction: number) => ({
      x: direction > 0 ? 1000 : -1000,
      opacity: 0,
      rotateY: direction > 0 ? 45 : -45,
    }),
    center: {
      x: 0,
      opacity: 1,
      rotateY: 0,
    },
    exit: (direction: number) => ({
      x: direction < 0 ? 1000 : -1000,
      opacity: 0,
      rotateY: direction < 0 ? 45 : -45,
    }),
  }

  return (
    <AnimatePresence>
      {isOpen && (
        <>
          {/* Backdrop */}
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            className="fixed inset-0 bg-black/80 backdrop-blur-sm z-50"
            onClick={onClose}
          />

          {/* Modal */}
          <div className="fixed inset-0 z-50 flex items-center justify-center p-2">
            <motion.div
              initial={{ scale: 0.95, opacity: 0 }}
              animate={{ scale: 1, opacity: 1 }}
              exit={{ scale: 0.95, opacity: 0 }}
              className="relative w-full h-full max-w-[96vw] max-h-[94vh]"
              style={{ perspective: 1000 }}
            >
              <GlassCard className="p-4 h-full flex flex-col">
                {/* Close button */}
                <button
                  onClick={onClose}
                  className="absolute top-2 right-2 w-8 h-8 rounded-full bg-white/10 flex items-center justify-center text-white hover:bg-white/20 transition-colors z-10"
                >
                  ✕
                </button>

                {/* Page indicator */}
                <div className="absolute top-2 left-2 text-xs text-muted-gray">
                  {parsedPages.length > 0 && `${currentPage + 1} / ${parsedPages.length}`}
                </div>

                {/* Error notification if AI failed - MOVED TO TOP */}
                {error && (
                  <div className="mt-8 mb-2 p-1.5 rounded-lg bg-mystical-gold/10 border border-mystical-gold/30 flex-shrink-0">
                    <p className="text-mystical-gold text-[10px] text-center leading-tight">
                      🌙✨ Звёзды немного устали... Читаем классику астрологии! 📜
                    </p>
                  </div>
                )}

                {/* Loading state */}
                {isLoading ? (
                  <div className="flex-1 flex flex-col items-center justify-center">
                    <LoadingSpinner size="lg" />
                    <p className="text-muted-gray mt-4">Генерируем ваше персональное толкование...</p>
                  </div>
                ) : parsedPages.length > 0 ? (
                  <>
                    {/* Page content with flip animation */}
                    <div className="flex-1 flex flex-col min-h-0 overflow-y-auto">
                      <AnimatePresence initial={false} custom={direction}>
                        <motion.div
                          key={currentPage}
                          custom={direction}
                          variants={pageVariants}
                          initial="enter"
                          animate="center"
                          exit="exit"
                          transition={{
                            x: { type: 'spring', stiffness: 300, damping: 30 },
                            opacity: { duration: 0.2 },
                            rotateY: { duration: 0.5 },
                          }}
                          className="h-full flex flex-col py-2"
                        >
                          {/* Title */}
                          <h2 className="text-2xl md:text-3xl font-display font-bold text-mystical-gold mb-4 text-center flex-shrink-0">
                            {currentPageData.title}
                          </h2>

                          {/* Content - scrollable */}
                          <div className="flex-1 overflow-y-auto px-2">
                            <p className="text-soft-white text-base md:text-lg leading-relaxed whitespace-pre-line">
                              {currentPageData.content}
                            </p>
                          </div>
                        </motion.div>
                      </AnimatePresence>
                    </div>

                    {/* Navigation */}
                    <div className="flex items-center justify-between pt-3 mt-3 border-t border-white/10 flex-shrink-0">
                      <button
                        onClick={prevPage}
                        disabled={currentPage === 0}
                        className="px-3 py-1.5 text-sm rounded-lg bg-white/10 hover:bg-white/20 disabled:opacity-30 disabled:cursor-not-allowed transition-colors"
                      >
                        ← Назад
                      </button>

                      <div className="flex gap-1">
                        {parsedPages.map((_, i) => (
                          <div
                            key={i}
                            className={`w-2 h-2 rounded-full transition-colors ${
                              i === currentPage ? 'bg-mystical-gold' : 'bg-white/20'
                            }`}
                          />
                        ))}
                      </div>

                      <button
                        onClick={nextPage}
                        disabled={currentPage === parsedPages.length - 1}
                        className="px-3 py-1.5 text-sm rounded-lg bg-white/10 hover:bg-white/20 disabled:opacity-30 disabled:cursor-not-allowed transition-colors"
                      >
                        Далее →
                      </button>
                    </div>
                  </>
                ) : null}
              </GlassCard>
            </motion.div>
          </div>
        </>
      )}
    </AnimatePresence>
  )
}
